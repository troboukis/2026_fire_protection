# Μεθοδολογία Συλλογής και Επεξεργασίας Δεδομένων

## Σκοπός και εύρος

Το project καταγράφει και αναλύει δημόσιες δαπάνες που σχετίζονται με την πυροπροστασία στην Ελλάδα. Στόχος είναι να αποτυπωθεί πόσα χρήματα δαπανούν δήμοι, περιφέρειες και άλλοι δημόσιοι φορείς για έργα, υπηρεσίες και εξοπλισμό που συνδέονται με την πρόληψη ή την αντιμετώπιση πυρκαγιών, καθώς και πώς αυτές οι δαπάνες κατανέμονται γεωγραφικά.

Η βάση δεδομένων καλύπτει εγγραφές από το 2024 και μετά. Υπάρχουν διαθέσιμα παλαιότερα δεδομένα (από το 2009), αλλά δεν χρησιμοποιούνται στην τρέχουσα έκδοση, ώστε η σύγκριση να γίνεται σε ένα πιο συνεκτικό και πρόσφατο χρονικό διάστημα.

Οι βασικές πηγές δεδομένων είναι:

- **ΚΗΜΔΗΣ** — συμβάσεις προμηθειών και υπηρεσιών
- **Διαύγεια** — αποφάσεις που σχετίζονται με δαπάνες και εμφανίζονται στο frontend μαζί με τις συμβάσεις
- **Copernicus EFFIS** — ιστορικά στοιχεία δασικών πυρκαγιών
- **Ελληνική Πυροσβεστική** — ενεργά συμβάντα πυρκαγιών σε πραγματικό χρόνο
- **Ειδησεογραφικά ρεπορτάζ** — πρόσφατα άρθρα για πυρκαγιές από Καθημερινή, News247, in.gr, ΕφΣυν και Πρώτο Θέμα
- **Χρηματοδοτήσεις** — κεντρικές κρατικές χρηματοδοτήσεις πυροπροστασίας ανά δήμο (Υπουργείο Εσωτερικών)

Τα δεδομένα από κάθε πηγή συλλέγονται ξεχωριστά και επεξεργάζονται σε διαδοχικά στάδια, ώστε να είναι δυνατός ο έλεγχος των ενδιάμεσων αποτελεσμάτων και να διατηρείται το αρχικό υλικό χωρίς αλλαγές.

---

## Πηγή 1: ΚΗΜΔΗΣ

### Συλλογή δεδομένων

Τα δεδομένα από το ΚΗΜΔΗΣ αντλούνται μέσω του OpenData API
`cerpp.eprocurement.gov.gr/khmdhs-opendata/contract`, με χρήση του script
`src/fetch_kimdis_procurements.py`.

Η αναζήτηση βασίζεται σε CPV codes (Common Procurement Vocabulary). Χρησιμοποιούνται δύο διαφορετικές ομάδες κωδικών.

**Πρωτεύοντες CPV** — σχετίζονται άμεσα με πυροπροστασία. Κρατούνται όλες οι συμβάσεις χωρίς επιπλέον φίλτρο:

- 75251100-1 — Υπηρεσίες καταπολέμησης πυρκαγιών
- 35111000-5 — Πυροσβεστικός εξοπλισμός
- 77312000-0 — Υπηρεσίες εκκαθάρισης από αγριόχορτα
- 77340000-5 — Κλάδεμα δέντρων και θάμνων
- και άλλοι σχετικοί κωδικοί που ορίζονται στο `PRIMARY_CPVS`

**Δευτερεύοντες CPV** — γενικότεροι κωδικοί που μπορεί να περιέχουν σχετικές συμβάσεις:

- 45233141-9 — Συντήρηση οδών
- 45240000-1 — Κατασκευαστικές εργασίες για υδατικά έργα
- 45232152-2 — Έργα αντιπλημμυρικής / αποχετευτικής υποδομής

Για τους δευτερεύοντες CPV εφαρμόζεται φίλτρο τίτλου: η εγγραφή κρατιέται μόνο αν ο τίτλος της σύμβασης περιέχει τη λέξη-κλειδί `πυροπροστασ`. Ο διαχωρισμός αυτός γίνεται επειδή γενικοί CPV χωρίς φίλτρο θα περιλάμβαναν μεγάλο αριθμό άσχετων συμβάσεων.

### Incremental συλλογή

Η συλλογή γίνεται σταδιακά. Το script κρατά την τελευταία ημερομηνία που έχει ήδη ανακτηθεί στο αρχείο `state/kimdis_state.json`. Σε κάθε νέο run ζητά μόνο εγγραφές μετά από αυτή την ημερομηνία. Αν το state file δεν υπάρχει, η ημερομηνία εκκίνησης υπολογίζεται από τη μεγαλύτερη `submissionDate` που υπάρχει ήδη στο CSV.

Το API έχει περιορισμούς στο εύρος ημερομηνιών, οπότε τα requests γίνονται σε χρονικά παράθυρα (default 180 ημερών). Για πλήρη επαναφόρτωση χρησιμοποιείται το flag `--full-refresh`. Σε κάθε εκτέλεση αποθηκεύεται backup των raw αντικειμένων σε JSON (`data/raw_items_backup.json`), ώστε να μπορεί να ξαναχτιστεί το CSV χωρίς νέο API call (`--from-backup`).

### Αλυσίδες συμβάσεων (prevReferenceNo / nextRefNo)

Το ΚΗΜΔΗΣ χρησιμοποιεί αλυσίδες συμβάσεων όπου μια παλαιότερη σύμβαση τροποποιείται ή αντικαθίσταται από νεότερη με νέο `referenceNumber`. Η λογική αντιμετώπισης:

- Το raw CSV παραμένει αναλλοίωτο για auditability.
- Κατά το ingest, το `payment.amount_without_vat` μηδενίζεται για superseded συμβάσεις — αυτές που εμφανίζονται ως `prevReferenceNo` σε νεότερη εγγραφή ή έχουν μη κενό `nextRefNo`.
- Μόνο η τελευταία σύμβαση της αλυσίδας έχει νομισματικό βάρος στα aggregates και στο frontend.

### Εντοπισμός και γεωκωδικοποίηση έργων (`locate_work`)

Για υποσύνολο συμβάσεων επιχειρείται εντοπισμός του ακριβούς σημείου εργασίας μέσα στο έγγραφο. Η διαδικασία υλοποιείται με το `src/run_locate_work_updates.py` και την κλάση `Document` στο `src/locate_work.py`.

Επιλέγονται συμβάσεις που:

- έχουν `reference_number`
- δεν έχουν ήδη εγγραφή στον πίνακα `works`
- ανήκουν στο τρέχον έτος
- προέρχονται κυρίως από δήμους, περιφέρειες ή ΑΔΜΗΕ

Για κάθε σύμβαση γίνεται λήψη του PDF από το ΚΗΜΔΗΣ. Το κείμενο εξάγεται με native PDF parsing και, όταν μια σελίδα δεν περιέχει αναγνώσιμο κείμενο, χρησιμοποιείται OCR ως fallback. Το έγγραφο αποστέλλεται σε LLM prompt που εντοπίζει σαφείς αναφορές σε συγκεκριμένα σημεία και εργασίες πυροπροστασίας (αποψιλώσεις, κλαδέματα, καθαρισμούς, αντιπυρικές ζώνες κ.λπ.).

Για κάθε εύρημα παράγονται τα πεδία `point_name_raw`, `point_name_canonical`, `work`, `page`, `excerpt`. Γίνεται deduplication και στη συνέχεια το `point_name_canonical` στέλνεται στο Google Geocoding API. Τα αποτελέσματα (`lat`, `lon`, `formatted_address`, `place_id`) αποθηκεύονται στον πίνακα `public.works`, που συνδέεται με τη σύμβαση μέσω `reference_number`. Για το frontend υπάρχει το view `public.works_enriched`.

---

## Πηγή 2: Διαύγεια

> **Κατάσταση:** Η συλλογή και η επεξεργασία δεδομένων Διαύγειας είναι λειτουργικά. Στο frontend εμφανίζονται τόσο σε ξεχωριστή σελίδα Διαύγειας όσο και στην αρχική σελίδα μαζί με τις πιο πρόσφατες συμβάσεις.

### Συλλογή δεδομένων

Από τη Διαύγεια συλλέγονται αποφάσεις μέσω του search API
`diavgeia.gov.gr/luminapi/api/search`, με χρήση λέξεων-κλειδιών που σχετίζονται με πυροπροστασία. Τα αποτελέσματα μπορεί να περιλαμβάνουν εγκρίσεις δαπάνης, αναλήψεις υποχρέωσης, απευθείας αναθέσεις, πληρωμές και αποφάσεις κατακύρωσης.

Για κάθε αποτέλεσμα γίνεται δεύτερο request στο endpoint `/decisions/view/{ada}` για δομημένα μεταδεδομένα (ποσά, ΑΦΜ δικαιούχων, υπογράφοντες, ΚΑΕ/ΑΛΕ, ημερομηνίες). Τα στοιχεία αποθηκεύονται σε ξεχωριστές στήλες ανά τύπο απόφασης (`spending_*`, `commitment_*`, `direct_*`, `payment_*`).

Η συλλογή από τη Διαύγεια εκτελείται αυτόματα σε καθημερινή βάση μέσω GitHub Actions (03:00 UTC).

### PDF pipeline

Τα PDFs αποθηκεύονται τοπικά στον φάκελο `pdf/` (εξαιρούνται από το git). Το pipeline (`src/pdf_pipeline.py`) κάνει δύο βήματα: κατεβάζει τα PDFs που λείπουν από το `documentUrl`, και τα αναλύει για να χτίσει το `data/pdf_pages_dataset.csv` — ένα αρχείο με μία εγγραφή ανά PDF που περιέχει το πλήρες κείμενο.

Το PDF pipeline εκτελείται τοπικά και δεν τρέχει σε CI (GitHub Actions), επειδή τα αρχεία PDF είναι μεγάλα και ακατάλληλα για αυτοματοποίηση.

### Relevance filter

Το `src/filter_relevance.py` ελέγχει κάθε εγγραφή ως προς τη σχετικότητά της χρησιμοποιώντας:

- `subject_match` — λέξεις-κλειδιά στο subject της απόφασης
- `pdf_match` — λέξεις-κλειδιά στο κείμενο του PDF (αναζητείται μέσω in-memory lookup `ada → text`)

Αν τουλάχιστον ένα από τα δύο είναι θετικό, η εγγραφή σημειώνεται `is_relevant = True`. Δεν εφαρμόζεται scoring — η λογική είναι απλό boolean OR.

Εξαγωγές:
- Ενημερώνει το ενδιάμεσο αρχείο `data/2026_diavgeia.csv` με τις στήλες relevance (`subject_match`, `pdf_match`, `pdf_available_for_filter`, `is_relevant`, `matched_keywords_subject`, `matched_keywords_pdf`)
- Παράγει το `data/2026_diavgeia_filtered.csv` (μόνο `is_relevant == True`)

Το `2026_diavgeia_filtered.csv` χρησιμοποιείται ως ενδιάμεσο αρχείο για το ingest στη βάση (`public.diavgeia`).

Το κείμενο κανονικοποιείται πριν τη σύγκριση: lowercase, αφαίρεση τόνων, εξομάλυνση του τελικού σίγμα (ς→σ), αντικατάσταση σημείων στίξης με κενά.

---

## Πηγή 3: Copernicus EFFIS

Τα δεδομένα για δασικές πυρκαγιές προέρχονται από το EFFIS API της υπηρεσίας
Copernicus Emergency Management Service: `api.effis.emergency.copernicus.eu`

Κάθε εγγραφή αντιστοιχεί σε ένα συμβάν πυρκαγιάς και περιλαμβάνει `firedate`, `area_ha`, `shape`/`centroid`, `lastupdate`, στοιχεία κάλυψης γης και ποσοστό εντός Natura 2000.

### Incremental fetch

Η συλλογή είναι incremental: το script αποθηκεύει το μέγιστο `lastupdate` από τη βάση και το χρησιμοποιεί ως σημείο εκκίνησης για το επόμενο run, με μικρό lookback window για αναδρομικές διορθώσεις. Αν η βάση είναι κενή, γίνεται πλήρης φόρτωση.

### Αντιστοίχιση σε δήμους

Κάθε πυρκαγιά αντιστοιχίζεται σε δήμο μέσω spatial join με τα όρια δήμων (GeoJSON):

1. Αν υπάρχει πολύγωνο, χρησιμοποιείται το shape
2. Αν δεν υπάρχει, χρησιμοποιείται το centroid
3. Γίνεται reprojection σε EPSG:2100
4. Υπολογίζεται επικάλυψη με δήμους

Αποθηκεύονται `municipality_key`, `overlap_ratio`, `match_method`. Τα αποτελέσματα φορτώνονται στη βάση και εμφανίζονται στο frontend μέσω του `FireCopernicusSection` component ως χορογραφικός χάρτης.

---

## Πηγή 4: Ενεργά συμβάντα πυρκαγιών (Ελληνική Πυροσβεστική)

Τα ενεργά συμβάντα πυρκαγιών αντλούνται σε πραγματικό χρόνο από τη σελίδα:
`https://www.fireservice.gr/el/energa-symvanta/`

Scraper: `src/scrape_forest_fires.py`

Αποθηκεύονται στον πίνακα `public.current_fires`. Στο frontend εμφανίζονται μόνο ενεργά συμβάντα (εξαιρούνται εγγραφές με status `ΛΗΞΗ`) στον fire ticker της αρχικής σελίδας. Το ίδιο dataset χρησιμοποιείται και από το news ticker για να επισημαίνει άρθρα που αντιστοιχούν σε ενεργή πυρκαγιά και να ενεργοποιεί το ίδιο hover highlight στον χάρτη με το fire ticker.

Όταν το ενσωματωμένο iframe των ενεργών συμβάντων επιστρέφει HTTP 500, ενεργοποιείται
αυτόματα εναλλακτική ροή από τον επίσημο λογαριασμό `@pyrosvestiki` στο X. Η ροή:

1. διαβάζει μόνο αναρτήσεις του τελευταίου 24ώρου και εξαιρεί retweets,
2. απορρίπτει διασώσεις, προγνώσεις κινδύνου και συγκεντρωτικές ανακοινώσεις,
3. ακολουθεί reply threads ώστε οι ενημερώσεις δυνάμεων ή κατάστασης να ενημερώνουν το ίδιο συμβάν,
4. εξάγει μία συγκεκριμένη περιοχή και τον τύπο βλάστησης με LLM,
5. γεωκωδικοποιεί την περιοχή και την αντιστοιχίζει γεωχωρικά σε δήμο,
6. αποθηκεύει το συμβάν στο `public.current_fires` με διακριτή προέλευση και σύνδεσμο προς το post.

Οι πυρκαγιές αυτής της εναλλακτικής πηγής παραμένουν ενεργές για 24 ώρες από τη νεότερη
σχετική ανάρτηση. Μετά το όριο αποσύρονται αυτόματα από τους ενεργούς δείκτες του χάρτη,
επειδή ο λογαριασμός δεν δημοσιεύει πάντοτε ανακοίνωση λήξης για κάθε συμβάν. Στα tooltips
εμφανίζεται η ένδειξη «Πηγή: Πυροσβεστικό Σώμα στο X (@pyrosvestiki)».

---

## Πηγή 5: Ειδησεογραφικά ρεπορτάζ πυρκαγιών

Τα πρόσφατα ρεπορτάζ συλλέγονται από τις ροές Καθημερινή, News247, in.gr, ΕφΣυν και Πρώτο Θέμα με το script `src/fetch_news_fires.py`. Η ροή είναι incremental και χρησιμοποιεί το `logs/news_fires_state.json` ως state αρχείο, ώστε οι ίδιες σελίδες καταλόγου να μην ξανασκανάρονται σε κάθε εκτέλεση.

Η συλλογή λειτουργεί σε στάδια:

1. Ανάγνωση σελίδων καταλόγου ανά πηγή μέχρι να βρεθεί το προηγούμενο boundary article.
2. Φίλτρο τίτλου για όρους όπως `πυρκαγιά`, `φωτιά`, `εμπρησμό`.
3. Ανάγνωση σώματος άρθρου μόνο για πιθανά σχετικά αποτελέσματα.
4. Δευτερεύον φίλτρο σώματος για ενδείξεις πραγματικής πυρκαγιάς, π.χ. Πυροσβεστική, δασική έκταση, υπό έλεγχο, ρίψεις νερού.
5. LLM extraction μίας γεωγραφικής περιοχής για το άρθρο.
6. Google Geocoding και αντιστοίχιση σε δήμο με point-in-polygon στα δημοτικά όρια.

Κάθε άρθρο αποθηκεύεται ως μία εγγραφή στον πίνακα `public.news_fires`, με μοναδικότητα στο `article_url`. Τα πεδία περιλαμβάνουν τίτλο, πηγή, URL άρθρου, image URL, χρόνο δημοσίευσης/συλλογής, περιοχή, geocode query, συντεταγμένες και αντιστοιχισμένο δήμο όταν υπάρχει αξιόπιστη αντιστοίχιση.

Στο frontend, το `NewsTicker` component εμφανίζει ρεπορτάζ στην αρχική σελίδα. Η επιλογή των άρθρων γίνεται server-side από το RPC `get_news_ticker_articles()`:

- αν υπάρχουν 5 ή περισσότερα σημερινά άρθρα, εμφανίζονται όλα τα σημερινά άρθρα
- αν τα σημερινά άρθρα είναι λιγότερα από 5, εμφανίζονται τα τελευταία 10 άρθρα συνολικά
- για κάθε άρθρο επιστρέφεται και `active_fire_incident_key` όταν ο δήμος του άρθρου έχει ενεργή πυρκαγιά στο `public.current_fires`

Το ticker ακούει realtime αλλαγές στο `public.news_fires` και στο `public.current_fires`. Όταν αλλάζουν τα άρθρα ή οι ενεργές πυρκαγιές, ανανεώνει τα δεδομένα μέσω του RPC. Τα αρχικά αποτελέσματα φορτώνονται με το ίδιο homepage cache/retry/stale fallback pattern που χρησιμοποιούν και άλλα homepage RPCs.

---

## Πηγή 6: Χρηματοδοτήσεις πυροπροστασίας

Κεντρικές κρατικές χρηματοδοτήσεις πυροπροστασίας ανά δήμο εισάγονται στον πίνακα `public.fund` της Supabase βάσης. Τα δεδομένα περιλαμβάνουν τακτικές, έκτακτες, δημοτικές και συνδεσμικές κατηγορίες δαπανών.

Η συγκριτική ανάλυση χρηματοδοτήσεων εμφανίζεται στο frontend μέσω του `Funding` component, που εμφανίζει ιστορικό ανά έτος και σύγκριση τρέχοντος έτους έναντι προηγούμενου.

---

## Pipeline επεξεργασίας

Το pipeline ακολουθεί διαδοχικά layers:

```
1. Συλλογή raw δεδομένων (ενδιάμεσα αρχεία)
   fetch_kimdis_procurements.py   → data/raw_procurements.csv (ενδιάμεσο)
   fetch_diavgeia.py              → data/2026_diavgeia.csv (ενδιάμεσο)
   src/fetch_copernicus.py        → public.copernicus_fire (απευθείας στη βάση)
   src/scrape_forest_fires.py     → public.current_fires (απευθείας στη βάση)
   src/fetch_news_fires.py        → public.news_fires (απευθείας στη βάση)

2. PDF pipeline (τοπικά μόνο)
   src/pdf_pipeline.py            → pdf/ + data/pdf_pages_dataset.csv (ενδιάμεσο)

3. Relevance filter (τοπικά μόνο)
   src/filter_relevance.py        → data/2026_diavgeia_filtered.csv (ενδιάμεσο)

4. Copernicus spatial join
   src/map_copernicus_to_municipalities.py → ενημέρωση public.copernicus_fire

5. DB ingest (πηγή αλήθειας: Supabase/Postgres)
   ingest/stage2_load_erd.py      → public.procurement, payment, cpv,
                                     diavgeia, beneficiary, organization,
                                     municipality, region, fund, forest_fire

6. Locate work (τοπικά, επί ζήτηση)
   src/run_locate_work_updates.py → public.works

7. Frontend
   React + Supabase RPC
```

### Αυτόματη εκτέλεση (GitHub Actions)

Η καθημερινή αυτόματη ροή (`scripts/run_fetch_and_sync.sh`, 03:00 UTC) εκτελεί:
- Συλλογή Διαύγεια + ΚΗΜΔΗΣ + Copernicus
- DB ingest (αν `RUN_DB_INGEST=1`)
- Αυτόματο commit και push των αρτεφάκτων (`data/`, `state/`, `logs/`)

Η ροή ειδησεογραφικών πυρκαγιών εκτελείται ξεχωριστά μέσω `scripts/run_news_fires_and_push_state.sh` και του workflow `.github/workflows/news-fires-refresh.yml`. Το workflow απαιτεί `DATABASE_URL`, `OPENAI_API_KEY` και `GOOGLE_GEOCODING_API_KEY`, ενημερώνει το `public.news_fires` και κάνει commit μόνο το `logs/news_fires_state.json` όταν αλλάξει το state.

Τα βήματα PDF pipeline, relevance filter και locate work εκτελούνται τοπικά και δεν τρέχουν σε CI.

---

## Βάση δεδομένων (Supabase/Postgres)

Η Supabase/Postgres βάση είναι η μοναδική πηγή αλήθειας για το frontend. Τα CSV αρχεία στον φάκελο `data/` είναι ενδιάμεσα pipeline artifacts — δεν τα διαβάζει ποτέ το frontend απευθείας. Κάθε αλλαγή στα δεδομένα γίνεται ορατή στο webapp μόνο μετά από DB ingest.

Το ERD ορίζεται στο `sql/001_schema_erd.sql`. Οι κεντρικοί πίνακες:

| Πίνακας | Περιγραφή |
|---|---|
| `procurement` | Μία εγγραφή ανά σύμβαση ΚΗΜΔΗΣ |
| `payment` | Ποσά ανά σύμβαση (1:1 με `procurement`) |
| `cpv` | CPV codes ανά σύμβαση (1:N) |
| `diavgeia` | Αποφάσεις Διαύγειας |
| `diavgeia_procurement` | Bridge πίνακας `diavgeia ↔ procurement` |
| `organization` | Φορείς (με παραλλαγές ονομάτων) |
| `municipality` | Δήμοι (με παραλλαγές ονομάτων) |
| `region` | Περιφέρειες |
| `beneficiary` | Δικαιούχοι πληρωμών |
| `works` | Γεωκωδικοποιημένα σημεία έργων |
| `fund` | Κεντρικές χρηματοδοτήσεις ανά δήμο/έτος |
| `forest_fire` | Ιστορικά στοιχεία πυρκαγιών |
| `copernicus_fire` | Δεδομένα EFFIS |
| `current_fires` | Ενεργά συμβάντα Πυροσβεστικής |
| `news_fires` | Ρεπορτάζ πυρκαγιών με γεωκωδικοποίηση και αντιστοίχιση δήμου |

### RPC functions (frontend API)

Όλα τα aggregates υπολογίζονται server-side σε Postgres RPC functions. Το frontend δεν κάνει raw queries — λαμβάνει έτοιμα σύνολα:

| Function | Σκοπός |
|---|---|
| `get_hero_section_data` | KPIs αρχικής σελίδας (τρέχον vs. προηγούμενα έτη) |
| `get_raw_procurements_cumulative_curve` | Σωρευτική καμπύλη δαπανών ανά έτος |
| `get_contract_analysis` | Ανάλυση συμβάσεων (τύποι, διαδικασίες, CPV, φορείς) |
| `get_direct_award_distribution` | Κατανομή αξιών απευθείας αναθέσεων (histogram) |
| `get_homepage_funding` | Χρηματοδοτήσεις αρχικής σελίδας |
| `get_latest_contract_cards` | Πρόσφατες συμβάσεις για την αρχική σελίδα |
| `get_diavgeia_page` | Σελιδοποιημένες αποφάσεις Διαύγειας και πρόσφατες αποφάσεις αρχικής |
| `get_news_ticker_articles` | Άρθρα news ticker και σύνδεση με ενεργές πυρκαγιές |
| `get_municipality_fire_protection_data` | Δεδομένα ανά δήμο |
| `get_municipality_map_spend` | Χάρτης δαπανών ανά δήμο |
| `get_region_contracts` | Συμβάσεις ανά περιφέρεια |

---

## Αρχές επεξεργασίας δεδομένων

### Το raw dataset δεν αλλάζει

Τα αρχεία raw διατηρούνται όπως ήρθαν από την πηγή. Οι διορθώσεις γίνονται μόνο σε επόμενα στάδια (ingest layer), ώστε να υπάρχει audit trail. Κάθε απόφαση καθαρισμού καταγράφεται στο `DATA_CLEANING_DECISIONS.md`.

### Deduplication

**Διαύγεια**: Incremental runs χρησιμοποιούν upsert επί `ada`. Νέες εγγραφές που δεν έχουν `ada` εισάγονται με `NOT EXISTS` guard.

**ΚΗΜΔΗΣ (raw CSV)**: Full-row dedupe μετά από κανονικοποίηση list/dict τιμών σε stable JSON strings. Στο ingest layer, η αποφυγή διπλοτύπων γίνεται με identity priority: `reference_number` → `diavgeia_ada` → `(contract_number, organization_key)` → fallback.

**Aggregates**: Υπολογίζονται μόνο σε `rn = 1` rows (via `ROW_NUMBER() OVER (PARTITION BY business_key ORDER BY id DESC)`).

### Ταξινόμηση φορέων

Οι φορείς ταξινομούνται με prefix matching σε κατηγορίες (ΔΗΜΟΣ, ΠΕΡΙΦΕΡΕΙΑ, ΥΠΟΥΡΓΕΙΟ κ.λπ.). Ονόματα που δεν αναγνωρίζονται καταλήγουν στην κατηγορία `ΑΛΛΟΣ ΦΟΡΕΑΣ` και χρειάζονται χειροκίνητο έλεγχο. Υπάρχει blacklist για φορείς εκτός scope που αφαιρούνται τόσο κατά τη συλλογή όσο και πριν την αποθήκευση (safety net).

### Fallback ποσών

Αν λείπει ανάλυση ποσών ανά γραμμή, χρησιμοποιείται το συνολικό ποσό της απόφασης.

### Κανονικοποίηση τίτλων

Το ίδιο normalization εφαρμόζεται σε τίτλους, φορείς και λέξεις-κλειδιά: lowercase, αφαίρεση τόνων, εξομάλυνση τελικού σίγμα, αγνόηση ειδικών χαρακτήρων. Αυτό επιτρέπει αντιστοίχιση ανεξαρτήτως τονισμού ή γραφής.

---

## Σχεδιαστικές επιλογές και performance

### Η βαριά επεξεργασία γίνεται στον server

Το frontend δεν μεταφέρει raw dataset. Όλα τα summaries υπολογίζονται σε Postgres RPC functions και επιστρέφονται έτοιμα.

### Dynamic year system

Τα έτη δεν είναι hardcoded. Ξεκινούν από `YEAR_START = 2024` και επεκτείνονται αυτόματα κάθε χρόνο — τόσο στο frontend όσο και στις RPC functions.

### Fair YTD σύγκριση

Οι συγκρίσεις μεταξύ ετών γίνονται στο ίδιο χρονικό παράθυρο YTD. Το τέλος του παραθύρου υπολογίζεται με `LEAST(τρέχουσα ημέρα, τελευταία ημέρα μήνα)` ώστε να μην επηρεάζουν δίσεκτα έτη.

### Indexes

Υπάρχουν indexes σε `contract_signed_date`, `firedate`, `municipality_key`, `lastupdate` για γρήγορες χρονολογικές και γεωγραφικές αναζητήσεις.

### Ονομασία δικαιούχων

Το όνομα δικαιούχου επιλέγεται με χρονολογική προτεραιότητα (ΚΗΜΔΗΣ `contractSignedDate` → Διαύγεια `publishTimestamp` → fallback πίνακας `beneficiary`), ώστε να εμφανίζεται πάντα η πιο πρόσφατη επωνυμία.

### Caching στο frontend

Το `app/src/lib/homepageRpcCache.ts` υλοποιεί in-memory/localStorage caching των RPC αποτελεσμάτων της αρχικής σελίδας, ώστε να αποφεύγονται επαναλαμβανόμενα requests κατά την πλοήγηση. Τα homepage RPCs χρησιμοποιούν retry και stale fallback όπου χρειάζεται. Το `NewsTicker` χρησιμοποιεί το ίδιο cache pattern στην αρχική φόρτωση, ενώ realtime αλλαγές από `news_fires` ή `current_fires` κάνουν άμεση ανανέωση χωρίς να περιμένουν να λήξει το cache.

---

## Frontend

Το webapp (`app/`) είναι React + TypeScript + Vite + Supabase. Οι κύριες σελίδες:

| Σελίδα | URL | Περιεχόμενο |
|---|---|---|
| Αρχική | `/` | Fire ticker, news ticker, hero KPIs, σωρευτική καμπύλη δαπανών, χάρτης, πρόσφατες συμβάσεις/Διαύγεια, χρηματοδοτήσεις, Copernicus πυρκαγιές |
| Συμβάσεις | `/contracts` | Browser συμβάσεων με φίλτρα |
| Ανάλυση | `/analysis` | Ανάλυση τύπων/διαδικασιών, κατανομή αξιών απευθείας αναθέσεων |
| Χάρτης | `/maps` | Χορογραφικός χάρτης Copernicus |
| Δήμοι | `/municipalities` | Πίνακας δαπανών ανά δήμο |
| Περιβάλλον | `/environment-ministry` | Dashboard Υπουργείου Περιβάλλοντος |

---

## Πιθανά προβλήματα και περιορισμοί

### Λάθη x100 στα ποσά

Αφορά αποφάσεις Διαύγειας. Σε ορισμένες εγγραφές τα ποσά έχουν καταχωρηθεί χωρίς δεκαδικά (π.χ. 875.524,00 αντί για 8.755,24). Υπάρχει validator (`src/validate_diavgeia_amounts.py`) που συγκρίνει το ποσό της εγγραφής με το κείμενο του PDF. Δεν εφαρμόζονται αυτόματες διορθώσεις — τα δεδομένα αποθηκεύονται στη βάση όπως ήρθαν από το API της Διαύγειας.

### Αποτυχία ανάγνωσης PDF

Όταν ένα PDF δεν κατεβαίνει ή δεν αναλύεται, το `pdf_match` είναι υποχρεωτικά `False`. Σε αυτές τις περιπτώσεις χρησιμοποιείται μόνο ο έλεγχος του subject ως fallback.

### Δευτερεύοντες CPV και υπερίληψη

Οι δευτερεύοντες CPV (π.χ. 45233141-9 Συντήρηση οδών) είναι γενικοί. Το φίλτρο τίτλου μειώνει τα άσχετα αποτελέσματα αλλά δεν τα εξαλείφει πλήρως.

### Ασυνέπεια ονομάτων φορέων

Δεν υπάρχει ενιαία γραφή. Φορείς που δεν αναγνωρίζονται καταλήγουν στην κατηγορία `ΑΛΛΟΣ ΦΟΡΕΑΣ` και χρειάζονται χειροκίνητο έλεγχο.

### Copernicus centroid fallback

Σε εγγραφές χωρίς πλήρες πολύγωνο χρησιμοποιείται μόνο το centroid. Αυτό μπορεί να δώσει λάθος αντιστοίχιση σε μεγάλες πυρκαγιές που εκτείνονται σε περισσότερους από έναν δήμους.

### Αλυσίδες συμβάσεων και ατελής πληροφορία

Αν μια νεότερη σύμβαση δεν έχει ακόμα ανακτηθεί, η παλαιότερη εμφανίζεται με πλήρες ποσό. Η κατάσταση διορθώνεται αυτόματα στο επόμενο incremental run.

---

## Testing

Η codebase περιλαμβάνει unit tests για τις βασικές λειτουργίες επεξεργασίας. Τα tests εκτελούνται με pytest (`testpaths = tests`) και αφορούν απομονωμένες συναρτήσεις, όχι end-to-end runs.

### Τι καλύπτουν τα tests

**ΚΗΜΔΗΣ** (`test_fetch_kimdis_procurements.py`)
- Normalization με αφαίρεση τόνων
- Business key priority (`referenceNumber` > `diavgeiaADA`)
- Dedupe λογική: κρατά το νεότερο row ανά business key

**Relevance filter** (`test_filter_relevance.py`)
- Unit test για PDF lookup (κρατά το μεγαλύτερο κείμενο για duplicate ADA)
- Integration test για `apply_relevance_filter`
- End-to-end test για `run_relevance_filter` (filtered CSV, raw CSV με νέες στήλες, run log)

**Copernicus** (`test_fetch_copernicus.py`)
- Bootstrap mode (κενή βάση)
- Incremental mode με lookback window
- Φίλτρο ημερομηνιών για invalid `firedate`
- Parsing helpers (bbox, JSON, booleans)

**Municipality mapping** (`test_map_copernicus_to_municipalities.py`)
- Ανάλυση `DATABASE_URL` σε edge cases

**Locate work** (`test_locate_work.py`)
- Deduplication πριν το geocoding (case-insensitive, συγχώνευση findings)
- Επιλογή canonical ονόματος

### Φιλοσοφία testing

Τα tests ελέγχουν συμπεριφορά, όχι implementation. Χρησιμοποιείται `monkeypatch` για απομόνωση εξωτερικών εξαρτήσεων (DB, API). Τα fixtures στο `tests/fixtures/` είναι ελεγχόμενα δείγματα δεδομένων.
